{
  "name": "epoxidovo-cron-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Cron Worker — daily lead follow-up trigger for epoxidovo.sk",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test:trigger": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.18.0"
  }
}
